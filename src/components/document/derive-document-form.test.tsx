import { describe, it, expect } from 'vitest';
import { DeriveDocumentForm } from './derive-document-form';

describe('DeriveDocumentForm', () => {
    it('should be defined', () => {
        expect(DeriveDocumentForm).toBeDefined();
    });
});